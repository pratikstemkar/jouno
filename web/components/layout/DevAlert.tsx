const DevAlert = () => {
    return (
        <div className="flex w-full bg-primary text-white text-sm py-2 justify-center items-center">
            <span>
                Website is under production. Things might not work as
                intentioned.
            </span>
        </div>
    );
};

export default DevAlert;
